<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="68dp"
    android:layout_marginTop="4dp"
    android:background="@drawable/bg_filter_row"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="12dp"
    android:paddingEnd="14dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:id="@+id/history_filter_avatar"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:src="@drawable/avatar_placeholder"
        app:civ_border_color="@color/pocket_outline"
        app:civ_border_width="1dp"
        app:civ_circle_background_color="@color/pocket_surface_variant" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/history_filter_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="@color/pocket_text_primary"
            android:textSize="16sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/history_filter_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:textColor="@color/pocket_text_secondary"
            android:textSize="12sp" />
    </LinearLayout>

    <ImageView
        android:id="@+id/history_filter_check"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_check"
        app:tint="@color/pocket_primary" />
</LinearLayout>
