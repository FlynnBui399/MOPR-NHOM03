<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_login_gradient"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    tools:context=".LoginActivity">

    <ImageView
        android:id="@+id/login_logo"
        android:layout_width="96dp"
        android:layout_height="96dp"
        android:contentDescription="@string/login_logo_description"
        android:src="@mipmap/ic_launcher_round"
        app:layout_constraintBottom_toTopOf="@id/login_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.35"
        app:layout_constraintVertical_chainStyle="packed" />

    <TextView
        android:id="@+id/login_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:letterSpacing="0"
        android:text="@string/login_title"
        android:textColor="@color/pocket_primary"
        android:textSize="36sp"
        app:layout_constraintBottom_toTopOf="@id/login_subtitle"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/login_logo" />

    <TextView
        android:id="@+id/login_subtitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center_horizontal"
        android:text="@string/login_subtitle"
        android:textColor="@color/pocket_text_secondary"
        android:textSize="14sp"
        app:layout_constraintBottom_toTopOf="@id/login_button_group"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/login_title" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/login_button_group"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layoutAnimation="@anim/layout_slide_up_fade_in"
        android:layout_marginBottom="48dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <com.example.pocket.ui.PocketButton
            android:id="@+id/google_sign_in_button"
            android:layout_width="0dp"
            android:layout_height="@dimen/pocket_button_height"
            android:stateListAnimator="@animator/scale_press"
            android:text="@string/login_google"
            android:textColor="@color/pocket_google_button_text"
            app:cornerRadius="@dimen/pocket_radius_md"
            app:icon="@drawable/ic_google"
            app:iconGravity="textStart"
            app:iconPadding="12dp"
            app:iconSize="24dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:pocketVariant="primary" />

        <com.example.pocket.ui.PocketButton
            android:id="@+id/phone_sign_in_button"
            android:layout_width="0dp"
            android:layout_height="@dimen/pocket_button_height"
            android:layout_marginTop="12dp"
            android:stateListAnimator="@animator/scale_press"
            android:text="@string/login_phone"
            android:textColor="@color/pocket_text_primary"
            app:cornerRadius="@dimen/pocket_radius_md"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/google_sign_in_button"
            app:pocketVariant="outline"
            app:strokeColor="@color/pocket_white_40"
            app:strokeWidth="1dp" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.constraintlayout.widget.ConstraintLayout>
